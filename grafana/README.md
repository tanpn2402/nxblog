## NOTICE before run must run this cmd
```
mkdir ./data
sudo chmod -R 777 ./data
```

## folder structure
./grafana
    ├── data                      # created by user with permission = 0777
    └── README.md